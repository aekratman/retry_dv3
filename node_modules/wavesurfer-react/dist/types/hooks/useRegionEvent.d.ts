import { Region, RegionEvents } from 'wavesurfer.js/plugins/regions';
import { EventListener } from '../types/EventListener.js';

type RegionEventListener = (region: Region, ...rest: Parameters<EventListener<RegionEvents, keyof RegionEvents>>) => void;

export type { RegionEventListener };
