'use strict';

const isRegionsPlugin = (plugin)=>{
    return "addRegions" in plugin || "getRegions" in plugin || "regions" in plugin && "regionsContainer" in plugin;
};

exports.isRegionsPlugin = isRegionsPlugin;
